# User state never belongs in the install directory. Both live in ~/.claude-adapt-rules/;
# these entries only catch runs that point CLAUDE_ADAPT_RULES_HOME back at the repo.
data/
rules/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
